Claude wire and background task mapping follow Comet at `a1adfde23448a0e04256931d64a7c72a95b11db7`.
The adjacent license preserves Comet's MIT notice.

The catalog fixture contains only models and commands from the existing Claude Code 2.1.258 initialize capture.
It proves discovery shape. It does not prove account model access or authenticated resume history.
The fake peer executes only Node fixture code. These tests never execute Claude.

Current wire evidence comes from read-only inspection of Claude Code 2.1.258.
Its binary SHA-256 is `704f1334ac65d3e89e1c6c1d7663293ad786a6166afdb71b5075337df630f976`.
Byte 179084224 defines top-level `command_lifecycle`, `command_uuid`, and `state`.
Byte 179050578 defines `stream_event.user_message_uuid` on the first non-ping stream frame.
Bytes 190014065 and 190015643 advertise the checked `msg_lifecycle_v1` profile and version.
The adapter uses this exact profile for UUID-less automatic work.
Other versions need positive ownership evidence or remain subject to the bounded attribution timeout.
